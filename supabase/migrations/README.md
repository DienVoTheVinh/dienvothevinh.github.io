# Migrations

No migration SQL was fabricated during workspace setup. `../snapshots/current.json` records the migration versions and names reported by the linked Supabase project. Add future migrations only through the reviewed Supabase CLI workflow described in `../../DEVELOPMENT.md`.
