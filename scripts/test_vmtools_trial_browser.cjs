const assert=require('assert/strict');const {chromium}=require('C:/Users/PC/.cache/codex-runtimes/codex-primary-runtime/dependencies/node/node_modules/playwright');
(async()=>{const b=await chromium.launch({channel:'chrome',headless:true});try{for(const trial of [false,true]){const p=await b.newPage();await p.route('**/functions/v1/vmtools-license',r=>r.fulfill({json:{files:[],access:{},trial:{allowed:trial}}}));await p.route('**/vmtools/',r=>r.fulfill({body:'<h1>Isolated trial workspace</h1>',contentType:'text/html'}));await p.goto('http://127.0.0.1:8787/vmtool');await p.waitForSelector('[data-open-web]');await p.locator('#use-web').click();if(trial)assert.equal(await p.locator('iframe.vm-web-workspace').count(),1);else {await p.waitForURL('**/dang-nhap?redirect=vmtool');assert.match(p.url(),/dang-nhap/);}await p.close();}console.log('PASS guest web trial enabled/disabled portal routing');}finally{await b.close()}})().catch(e=>{console.error(e);process.exitCode=1});
